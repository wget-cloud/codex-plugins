interface:
  display_name: "WGC Bugfix"
  short_description: "Agent-driven bug diagnosis and verified fixes"
  default_prompt: "Use $wgc-bugfix to investigate, reproduce, fix, review, and verify this Wget Cloud bug."
