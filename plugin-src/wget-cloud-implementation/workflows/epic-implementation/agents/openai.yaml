interface:
  display_name: "WGC Epic Implementation"
  short_description: "Implement YouTrack epics and task pools safely"
  default_prompt: "Use $wgc-epic-implementation to implement this epic or task pool from the selected YouTrack in dependency order."

dependencies:
  tools:
    - type: "mcp"
      value: "youtrack"
      description: "Wget Cloud YouTrack MCP; personal authentication required"
