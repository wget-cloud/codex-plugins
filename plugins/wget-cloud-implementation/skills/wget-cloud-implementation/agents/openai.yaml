interface:
  display_name: "Wget Cloud Implementation"
  short_description: "Architecture-safe multi-agent delivery for Wget Cloud"
  default_prompt: "Use $wget-cloud-implementation to implement this task across Wget Cloud with architecture, tests, review, QA, and GitOps gates."
